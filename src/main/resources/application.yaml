spring:
  liquibase:
    enabled: true
  datasource:
    password: postgres
    username: postgres
    url: jdbc:postgresql://localhost:5432/postgres
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
springdoc:
  swagger-ui:
    path: /swagger
    tryItOutEnabled: true
